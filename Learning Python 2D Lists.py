number_grid = [[1, 2, 3],
               [4, 5, 6],
               [7, 8, 9],
               [0]
]

# print(number_grid[0][0])

# In Python, we can create a grid-like structure (in this case, 4x3)
# When we print out our 2D list, we can specify row x column = variable[row][column]

for row in number_grid:
    print(row)